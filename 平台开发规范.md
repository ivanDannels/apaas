# 统一智能数据应用平台(UDAP) - 开发规范

## 1. 代码规范

### 1.1 编程语言规范

#### 1.1.1 Java代码规范

##### 1.1.1.1 命名规范
- **包命名**: 全部小写，用点分隔，如`com.company.project.module`
- **类命名**: 大驼峰命名法，如`UserService`
- **接口命名**: 大驼峰命名法，如`UserService`
- **方法命名**: 小驼峰命名法，如`getUserById`
- **变量命名**: 小驼峰命名法，如`userName`
- **常量命名**: 全大写，下划线分隔，如`MAX_COUNT`
- **枚举命名**: 大驼峰命名法，如`UserRole`

##### 1.1.1.2 代码格式
- **缩进**: 使用4个空格缩进，不使用Tab
- **行宽**: 每行不超过120个字符
- **换行**: 大括号不换行，如`if (condition) {`
- **空格**: 
  - 关键字后加空格，如`if (condition)`
  - 二元运算符前后加空格，如`a = b + c`
  - 逗号后加空格，如`method(a, b, c)`

##### 1.1.1.3 注释规范
- **类注释**: 使用JavaDoc格式，包含@author、@version、@since等标签
- **方法注释**: 使用JavaDoc格式，包含@param、@return、@throws等标签
- **行内注释**: 使用`//`进行简短说明
- **块注释**: 使用`/* */`进行多行说明

##### 1.1.1.4 异常处理
- **检查性异常**: 必须处理或声明抛出
- **运行时异常**: 合理捕获和处理
- **自定义异常**: 继承合适的异常类，提供有意义的错误信息
- **日志记录**: 异常发生时记录详细日志

##### 1.1.1.5 日志规范
- **日志框架**: 使用SLF4J + Logback
- **日志级别**: 
  - ERROR: 错误信息，需要立即处理
  - WARN: 警告信息，需要注意
  - INFO: 一般信息，用于记录流程
  - DEBUG: 调试信息，用于开发调试
  - TRACE: 跟踪信息，最详细的日志
- **日志格式**: 包含时间、级别、线程、类名、消息
- **敏感信息**: 不在日志中记录密码、密钥等敏感信息

#### 1.1.2 JavaScript/TypeScript代码规范

##### 1.1.2.1 命名规范
- **变量命名**: 小驼峰命名法，如`userName`
- **函数命名**: 小驼峰命名法，如`getUserInfo`
- **类命名**: 大驼峰命名法，如`UserService`
- **常量命名**: 全大写，下划线分隔，如`MAX_COUNT`
- **文件命名**: 小写，连字符分隔，如`user-service.ts`

##### 1.1.2.2 代码格式
- **缩进**: 使用2个空格缩进
- **分号**: 语句结尾使用分号
- **引号**: 字符串使用单引号
- **对象属性**: 优先使用点号访问，如`user.name`

##### 1.1.2.3 注释规范
- **单行注释**: 使用`//`进行说明
- **多行注释**: 使用`/* */`进行说明
- **函数注释**: 使用JSDoc格式，包含@param、@return等标签

##### 1.1.2.4 ES6+特性使用
- **箭头函数**: 合理使用箭头函数
- **解构赋值**: 使用解构赋值简化代码
- **模板字符串**: 使用模板字符串拼接字符串
- **模块导入**: 使用ES6模块系统

#### 1.1.3 Vue.js代码规范

##### 1.1.3.1 组件命名
- **文件命名**: 大驼峰命名法，如`UserList.vue`
- **组件引用**: 使用大驼峰命名法，如`<UserList />`

##### 1.1.3.2 组件结构
```vue
<template>
  <!-- 模板内容 -->
</template>

<script>
// 脚本内容
export default {
  name: 'ComponentName',
  props: {},
  data() {
    return {}
  },
  computed: {},
  watch: {},
  methods: {},
  created() {},
  mounted() {}
}
</script>

<style scoped>
/* 样式内容 */
</style>
```

##### 1.1.3.3 组件属性
- **props**: 明确定义类型和默认值
- **data**: 函数返回对象
- **methods**: 方法命名使用小驼峰
- **computed**: 计算属性命名使用小驼峰

### 1.2 数据库规范

#### 1.2.1 命名规范
- **表名**: 小写，下划线分隔，如`user_info`
- **字段名**: 小写，下划线分隔，如`user_name`
- **索引名**: `idx_表名_字段名`，如`idx_user_info_name`
- **外键名**: `fk_表名_字段名`，如`fk_user_info_role_id`

#### 1.2.2 设计规范
- **主键**: 每表必须有主键，推荐使用自增ID或UUID
- **字段类型**: 合理选择字段类型，避免浪费存储空间
- **默认值**: 为字段设置合理的默认值
- **非空约束**: 明确标识字段是否允许为空
- **索引**: 为经常查询的字段建立索引

#### 1.2.3 SQL规范
- **关键字**: 全大写，如`SELECT`, `FROM`, `WHERE`
- **表别名**: 使用简短有意义的别名
- **字段别名**: 使用小驼峰命名法
- **条件顺序**: 按照选择性从高到低排列WHERE条件

## 2. 版本控制规范

### 2.1 Git使用规范

#### 2.1.1 分支命名
- **主分支**: `main` 或 `master`
- **开发分支**: `develop`
- **功能分支**: `feature/功能名称`，如`feature/user-login`
- **修复分支**: `hotfix/问题编号`，如`hotfix/bug-123`
- **发布分支**: `release/版本号`，如`release/v1.0.0`

#### 2.1.2 提交规范

##### 2.1.2.1 提交信息格式
```
<type>(<scope>): <subject>

<body>

<footer>
```

##### 2.1.2.2 提交类型
- **feat**: 新功能
- **fix**: 修复bug
- **docs**: 文档更新
- **style**: 代码格式调整（不影响代码运行）
- **refactor**: 代码重构
- **perf**: 性能优化
- **test**: 测试相关
- **chore**: 构建过程或辅助工具的变动

##### 2.1.2.3 提交示例
```
feat(user): 添加用户登录功能

实现用户登录接口，包括：
- 用户名密码验证
- JWT token生成
- 登录日志记录

Closes #123
```

#### 2.1.3 合并规范
- **Pull Request**: 所有代码合并必须通过PR
- **代码审查**: 至少一人审查通过后才能合并
- **自动化检查**: 通过CI检查后才能合并
- **删除分支**: 合并后删除功能分支

### 2.2 分支管理策略

#### 2.2.1 Git Flow模型
- **main分支**: 生产环境代码，只能通过release分支合并
- **develop分支**: 开发环境代码，功能分支从此分支创建
- **feature分支**: 功能开发分支，完成后合并回develop
- **release分支**: 发布分支，从develop创建，测试通过后合并到main和develop
- **hotfix分支**: 紧急修复分支，从main创建，修复后合并到main和develop

#### 2.2.2 分支保护规则
- **main分支**: 
  - 禁止直接推送
  - 必须通过PR合并
  - 必须通过CI检查
  - 至少一人审查通过
- **develop分支**: 
  - 禁止直接推送
  - 必须通过PR合并
  - 必须通过CI检查

## 3. 项目结构规范

### 3.1 后端项目结构

#### 3.1.1 Maven多模块结构
```
apaas-parent
├── apaas-common          # 公共模块
├── apaas-gateway         # 网关模块
├── apaas-auth            # 认证模块
├── apaas-user            # 用户模块
├── apaas-workflow        # 工作流模块
├── apaas-form            # 表单模块
├── apaas-integration     # 集成模块
├── apaas-report          # 报表模块
├── apaas-monitor         # 监控模块
└── apaas-job             # 任务模块
```

#### 3.1.2 模块内部结构
```
apaas-user
├── src
│   ├── main
│   │   ├── java
│   │   │   └── com.company.apaas.user
│   │   │       ├── controller    # 控制器层
│   │   │       ├── service       # 服务层
│   │   │       ├── repository    # 数据访问层
│   │   │       ├── entity        # 实体类
│   │   │       ├── dto           # 数据传输对象
│   │   │       ├── converter     # 数据转换器
│   │   │       ├── config        # 配置类
│   │   │       ├── exception     # 异常类
│   │   │       └── util          # 工具类
│   │   └── resources
│   │       ├── application.yml   # 配置文件
│   │       ├── mapper            # MyBatis映射文件
│   │       └── static            # 静态资源
│   └── test
│       └── java                  # 测试代码
└── pom.xml
```

### 3.2 前端项目结构

#### 3.2.1 Vue.js项目结构
```
apaas-ui
├── public
│   ├── index.html
│   └── favicon.ico
├── src
│   ├── assets        # 静态资源
│   ├── components    # 公共组件
│   ├── views         # 页面组件
│   ├── router        # 路由配置
│   ├── store         # 状态管理
│   ├── services      # API服务
│   ├── utils         # 工具函数
│   ├── styles        # 样式文件
│   ├── plugins       # 插件
│   ├── layouts       # 布局组件
│   └── App.vue
├── tests             # 测试文件
├── package.json
└── vue.config.js
```

## 4. API设计规范

### 4.1 RESTful API规范

#### 4.1.1 URL设计
- **资源命名**: 使用名词复数形式，如`/users`
- **层级关系**: 使用嵌套表示层级，如`/users/{id}/orders`
- **版本控制**: URL中包含版本号，如`/api/v1/users`
- **小写**: 所有URL使用小写

#### 4.1.2 HTTP方法
- **GET**: 查询资源
- **POST**: 创建资源
- **PUT**: 更新资源（全量更新）
- **PATCH**: 更新资源（部分更新）
- **DELETE**: 删除资源

#### 4.1.3 状态码
- **200**: 请求成功
- **201**: 创建成功
- **400**: 请求参数错误
- **401**: 未认证
- **403**: 无权限
- **404**: 资源不存在
- **500**: 服务器内部错误

#### 4.1.4 响应格式
```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "timestamp": "2023-01-01T00:00:00Z"
}
```

### 4.2 接口文档规范

#### 4.2.1 Swagger注解
- **@Api**: 描述Controller
- **@ApiOperation**: 描述接口
- **@ApiParam**: 描述参数
- **@ApiResponse**: 描述响应

#### 4.2.2 文档内容
- **接口描述**: 详细说明接口功能
- **请求参数**: 详细说明每个参数
- **响应示例**: 提供成功和失败的响应示例
- **错误码**: 列出可能的错误码和含义

## 5. 测试规范

### 5.1 测试分类

#### 5.1.1 单元测试
- **覆盖率**: 核心业务逻辑覆盖率不低于80%
- **框架**: 使用JUnit 5 + Mockito
- **命名**: 测试方法命名使用`should_期望结果_when_测试条件`格式
- **独立性**: 测试之间相互独立，无依赖关系

#### 5.1.2 集成测试
- **范围**: 测试模块间集成和外部依赖
- **环境**: 使用独立的测试环境
- **数据**: 使用测试数据，避免影响生产数据

#### 5.1.3 端到端测试
- **工具**: 使用Cypress或Selenium
- **场景**: 覆盖主要业务流程
- **自动化**: 集成到CI/CD流水线

### 5.2 测试代码规范

#### 5.2.1 命名规范
- **测试类**: 被测试类名 + `Test`，如`UserServiceTest`
- **测试方法**: `should_期望结果_when_测试条件`，如`should_ReturnUser_When_ValidId`

#### 5.2.2 代码结构
```java
@ExtendWith(MockitoExtension.class)
class UserServiceTest {
    
    @Mock
    private UserRepository userRepository;
    
    @InjectMocks
    private UserService userService;
    
    @Test
    void should_ReturnUser_When_ValidId() {
        // Given
        Long userId = 1L;
        User expectedUser = new User();
        when(userRepository.findById(userId)).thenReturn(Optional.of(expectedUser));
        
        // When
        User actualUser = userService.getUserById(userId);
        
        // Then
        assertThat(actualUser).isEqualTo(expectedUser);
    }
}
```

## 6. 安全规范

### 6.1 代码安全

#### 6.1.1 输入验证
- **参数校验**: 使用Bean Validation进行参数校验
- **SQL注入**: 使用预编译语句，避免拼接SQL
- **XSS攻击**: 对输出内容进行转义
- **文件上传**: 限制文件类型和大小，检查文件内容

#### 6.1.2 身份认证
- **JWT**: 使用JWT进行身份认证
- **过期时间**: 合理设置Token过期时间
- **刷新机制**: 实现Token刷新机制
- **存储安全**: 不在本地存储敏感Token

#### 6.1.3 权限控制
- **RBAC**: 基于角色的访问控制
- **方法级权限**: 使用Spring Security进行方法级权限控制
- **数据权限**: 实现行级和列级数据权限控制

### 6.2 数据安全

#### 6.2.1 数据传输
- **HTTPS**: 所有外部通信使用HTTPS
- **敏感数据**: 敏感数据传输时进行额外加密

#### 6.2.2 数据存储
- **密码加密**: 使用BCrypt加密存储密码
- **敏感信息**: 敏感信息加密存储
- **备份安全**: 备份数据加密存储

## 7. 性能规范

### 7.1 数据库优化

#### 7.1.1 索引优化
- **查询字段**: 为经常查询的字段建立索引
- **复合索引**: 合理使用复合索引
- **索引维护**: 定期检查和维护索引

#### 7.1.2 SQL优化
- **避免SELECT ***: 明确指定查询字段
- **分页查询**: 大数据量查询使用分页
- **连接查询**: 合理使用连接查询
- **子查询**: 避免不必要的子查询

### 7.2 缓存策略

#### 7.2.1 缓存使用
- **热点数据**: 缓存热点数据
- **缓存更新**: 实现合理的缓存更新策略
- **缓存穿透**: 使用布隆过滤器防止缓存穿透
- **缓存雪崩**: 设置不同的过期时间防止缓存雪崩

### 7.3 接口性能

#### 7.3.1 响应时间
- **一般接口**: 响应时间不超过200ms
- **复杂接口**: 响应时间不超过500ms
- **批量接口**: 响应时间不超过2000ms

#### 7.3.2 并发处理
- **线程池**: 合理使用线程池处理并发请求
- **异步处理**: 耗时操作使用异步处理
- **限流控制**: 实现接口限流控制

## 8. 日志规范

### 8.1 日志级别

#### 8.1.1 级别定义
- **ERROR**: 错误信息，需要立即处理
- **WARN**: 警告信息，需要注意
- **INFO**: 一般信息，用于记录流程
- **DEBUG**: 调试信息，用于开发调试
- **TRACE**: 跟踪信息，最详细的日志

### 8.2 日志内容

#### 8.2.1 格式规范
```
[时间] [级别] [线程] [类名] [消息] [异常堆栈]
```

#### 8.2.2 内容要求
- **时间**: 包含准确的时间戳
- **级别**: 明确的日志级别
- **线程**: 记录线程信息
- **类名**: 记录完整的类名
- **消息**: 清晰的描述信息
- **参数**: 使用参数化日志，避免字符串拼接

### 8.3 敏感信息

#### 8.3.1 信息过滤
- **密码**: 不在日志中记录密码
- **密钥**: 不在日志中记录密钥
- **个人信息**: 谨慎记录个人信息

## 9. 部署规范

### 9.1 环境配置

#### 9.1.1 配置文件
- **外部化配置**: 配置文件外部化管理
- **环境隔离**: 不同环境使用不同的配置文件
- **敏感信息**: 敏感配置信息加密存储

#### 9.1.2 启动参数
- **JVM参数**: 合理设置JVM参数
- **应用参数**: 通过启动参数传递配置

### 9.2 容器化部署

#### 9.2.1 Docker镜像
- **基础镜像**: 使用官方或可信的基础镜像
- **镜像大小**: 尽量减小镜像大小
- **安全扫描**: 定期进行安全扫描

#### 9.2.2 Kubernetes部署
- **资源配置**: 合理设置资源请求和限制
- **健康检查**: 配置存活和就绪探针
- **滚动更新**: 使用滚动更新策略

## 10. 文档规范

### 10.1 技术文档

#### 10.1.1 设计文档
- **架构设计**: 系统架构设计文档
- **数据库设计**: 数据库设计文档
- **接口设计**: API接口设计文档

#### 10.1.2 开发文档
- **环境搭建**: 开发环境搭建指南
- **编码规范**: 详细编码规范
- **测试指南**: 测试策略和指南

### 10.2 业务文档

#### 10.2.1 需求文档
- **功能需求**: 详细功能需求说明
- **非功能需求**: 性能、安全等非功能需求

#### 10.2.2 用户手册
- **操作指南**: 详细操作步骤
- **FAQ**: 常见问题解答

通过遵循以上开发规范，可以确保统一智能数据应用平台(UDAP)的代码质量、可维护性和团队协作效率。